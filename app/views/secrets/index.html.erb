<% @secrets.each do |secret| %>
  <blockquote>
		<h2>
			“<%= link_to secret.title, secret %>” 
			<%# ayto na to valw mono se kapoion admin poy einai log in %>
    </h2>
  </blockquote>
<% end %>  
<%= link_to "New Secret", new_secret_path, class: "btn btn-primary" %>