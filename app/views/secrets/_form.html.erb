<%= simple_form_for(@secret, html: { class:'form-horizontal' }) do |f| %>
	<%= f.input :title, label: "I want to tell that:" %><br/>
	<%= f.input :description, label: "This is the description (username)" %><br/>
	<%= f.button :submit %>
	<br>
<% end %>